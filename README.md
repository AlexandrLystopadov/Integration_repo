# 🎬 Cinema Management System (навчальний проєкт)

**Тема:** Система керування кінотеатром  
**Мова:** Python 3.x  
**IDE:** PyCharm  
**СКВ:** Git (репозиторій на GitHub)

---

## 📌 Опис проєкту
Проєкт призначений для демонстрації роботи з Git (гілки, pull request, merge, конфлікти) та реалізації ООП-підходу у Python через створення класів, що реалізують інтерфейси (через `abc.ABC`).

Система дозволяє:
- керувати **сеансами** (інформація, розклад, статус),
- виконувати **реєстрацію глядачів** на сеанс,
- **видавати квитки** та відстежувати їх **статус**.

---

## Вимоги (інтерфейси)
### 1) Інтерфейс `IScreening` (Сеанс)
Методи:
- `get_screening_info()` — інформація про сеанс
- `get_schedule()` — розклад (час початку)
- `get_status()` — статус сеансу

### 2) Інтерфейс `IViewer` (Глядач/Реєстрація)
Методи:
- `register_viewer(screening_id, full_name, seat)` — реєстрація глядача
- `issue_ticket(screening_id, full_name)` — видача квитка
- `list_viewers(screening_id)` — список глядачів

### 3) Інтерфейс `ITicket` (Квиток)
Методи:
- `get_ticket_info()` — інформація про квиток
- `mark_used()` — позначити як використаний
- `refund()` — повернення (анулювання)
- `get_status()` — статус квитка

---

## 🗂 Структура проєкту
cinema_management/
├─ cinema_system/
│ ├─ init.py
│ ├─ interfaces.py
│ ├─ screening.py
│ ├─ viewer.py
│ ├─ ticket.py
│ └─ main.py
├─ tests/
│ ├─ test_screening.py
│ └─ test_viewer_ticket.py
├─ run.py
├─ requirements.txt
├─ .gitignore
└─ README.md

---

##  Запуск проєкту
### 1) Створити та активувати віртуальне середовище
**Windows (PowerShell):**
```bash
python -m venv .venv
.venv\Scripts\Activate.ps1
Windows (cmd):

python -m venv .venv
.venv\Scripts\activate
Linux / macOS:


python -m venv .venv
source .venv/bin/activate
2) Встановити залежності

pip install -r requirements.txt
3) Запустити демонстрацію

python run.py
🧪 Запуск тестів

pytest -q
🌿 Робота з Git (приклад гілок)
Рекомендована структура гілок:

feature/screening — логіка сеансів

feature/viewer — реєстрація глядачів

feature/ticket — квитки та статуси

test/pytest — тести

👤 Автор
Листопадов Олександр Олексійович
Роль: програміст / тестувальник